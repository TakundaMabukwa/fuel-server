const express = require('express');
const router = express.Router();

// Placeholder routes - will be implemented when controllers are migrated
router.get('/', (req, res) => {
  res.json({ message: 'Energy Rite Vehicles routes - to be implemented' });
});

module.exports = router;